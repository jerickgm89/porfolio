---
interface Props {
  subtitle: string;
  center: boolean;
}

const { subtitle, center=false } = Astro.props;
---
<div>
  <p class={`text-2xl underline underline-offset-8 decoration-4 decoration-banner my-4 text-jblue ${center ? 'text-center' : ''}`}>
        {subtitle}
    </p>
</div>
<slot/>
